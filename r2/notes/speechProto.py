#  This is a json text

import json
states = open("gamestates.JSON", "r")
john = open("john.JSON")
states = states.get("game states")
current = states[1]


def getResponse(person):
    if(person.
print(
